deployment of a webpage using apache server on VM.

step1. make one ubuntu VM, EC2 instance using AWS.
step2. access the EC2 instance to host machine/local machine using SSH.
step3. now install apache packages to ubantu machine.
	- apt-get update
	- # apt-get install apache2
	- # /etc/init.d/apache2 start (hyper text transfer protocol deman)
	- #  /etc/init.d/apache2 status (Verify that the service is running by executing the following command)
	- # /etc/init.d/apache2 restart (Run the following command to restart Apache)

	- # mkdir /var/www/yourdomain.com (Create a directory for the website and then create index.html file for the website.)
	-

imp* apche config file path- /etc/httpd/conf/httpd.conf


